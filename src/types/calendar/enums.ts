// Calendar Type Enum
export enum CalendarType {
  PERSONAL = 'personal',
  TEAM = 'team',
}

// Calendar Collaborator Role Enum
export enum CalendarRole {
  ADMIN = 'admin',
  CONTRIBUTOR = 'contributor',
}
